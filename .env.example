# Canvas for Copilot — environment (all optional for local use)
# Server log verbosity: error | warn | info | debug
CANVAS_LOG_LEVEL=info
# Optional local-debug WebSocket fallback port (non-primary path)
CANVAS_DEBUG_WS_PORT=4317
